# ドキュメント整合性チェック結果 テンプレート

このテンプレートはドキュメント整合性チェック結果の出力フォーマットです。

---

## ドキュメント整合性チェック結果

### 対象ドキュメント

| ドキュメント | パス                                   | 最終更新日      |
|:-------|:-------------------------------------|:-----------|
| PRD    | `.sdd/requirement/{機能名}.md`          | YYYY-MM-DD |
| spec   | `.sdd/specification/{機能名}_spec.md`   | YYYY-MM-DD |
| design | `.sdd/specification/{機能名}_design.md` | YYYY-MM-DD |

### チェック結果サマリー

| チェック対象        | 結果             | 件数    |
|:--------------|:---------------|:------|
| PRD ↔ spec    | 🟢 整合 / 🔴 不整合 | {n} 件 |
| spec ↔ design | 🟢 整合 / 🔴 不整合 | {n} 件 |
| design ↔ 実装   | 🟢 整合 / 🔴 不整合 | {n} 件 |

---

### 🔴 不整合の詳細

#### PRD ↔ spec

##### 1. {不整合のタイトル}

**種別**: 欠落 / 矛盾 / 陳腐化

**PRDの記載**:

```markdown
{PRDの該当箇所}
```

**specの記載**:

```markdown
{specの該当箇所（または「記載なし」）}
```

**推奨アクション**:

- [ ] specを更新して要求を反映
- [ ] PRDの要求が不要なら削除

---

#### spec ↔ design

##### 1. {不整合のタイトル}

**種別**: 欠落 / 矛盾 / 陳腐化

**specの記載**:

```
{specの該当箇所}
```

**designの記載**:

```
{designの該当箇所（または「記載なし」）}
```

**推奨アクション**:

- [ ] designを更新して仕様を反映
- [ ] specの仕様が古いなら更新

---

#### design ↔ 実装

##### 1. {不整合のタイトル}

**種別**: 欠落 / 矛盾 / 陳腐化

**designの記載**:

```
{designの該当箇所}
```

**実装**:

```
{実際のファイル構造・コード}
```

**推奨アクション**:

- [ ] designを実装に合わせて更新
- [ ] 実装をdesignに合わせて修正

---

### 🟢 整合確認済み項目

- {確認済み項目1}
- {確認済み項目2}

---

### 推奨アクション（優先順位付き）

1. **高優先度**: {アクション}
2. **中優先度**: {アクション}
3. **低優先度**: {アクション}

---

### 備考

- {補足事項}
