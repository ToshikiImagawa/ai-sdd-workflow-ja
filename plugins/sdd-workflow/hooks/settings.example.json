{
  "$comment": [
    "AI-SDD Workflow Hooks Configuration",
    "",
    "Setup instructions:",
    "1. Copy this file to ~/.claude/settings.json (merge if existing)",
    "2. Copy hooks directory to your project root:",
    "   cp -r plugins/sdd-workflow/hooks /path/to/your/project/",
    "3. Grant execute permission to scripts:",
    "   chmod +x /path/to/your/project/hooks/*.sh",
    "4. Adjust paths to match your project (if necessary)",
    "",
    "Environment variables (set by SessionStart):",
    "  - SDD_DOCS_ROOT: Documentation root (default: .sdd)",
    "  - SDD_REQUIREMENT_DIR: Requirement directory (default: requirement)",
    "  - SDD_SPECIFICATION_DIR: Specification directory (default: specification)",
    "  - SDD_TASK_DIR: Task log directory (default: task)",
    "  - SDD_REQUIREMENT_PATH: Full requirement path (default: .sdd/requirement)",
    "  - SDD_SPECIFICATION_PATH: Full specification path (default: .sdd/specification)",
    "  - SDD_TASK_PATH: Full task log path (default: .sdd/task)",
    "",
    "Note: The $comment field is a conventional JSON comment.",
    "      Claude Code ignores this field."
  ],
  "hooks": {
    "SessionStart": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "./hooks/session-start.sh"
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "./hooks/check-spec-exists.sh"
          }
        ]
      }
    ]
  }
}
